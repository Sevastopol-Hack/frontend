enum RoutePaths {
  HOME = "/",
  SIGNUP = "/signup",
  LOGIN = "/login",
  EDIT_RESUME = "/resume/edit/:id",
  RESUME = "/resume/:id",
  VACANCIES = "/vacancies",
  CREATE_RESUME = "/resume/create",
}

export default RoutePaths;
