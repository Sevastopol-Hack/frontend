enum RoutePaths {
  HOME = "/",
  SIGNUP = "/signup",
  LOGIN = "/login",
  EDIT_RESUME = "/resume/edit",
  RESUME = "/resume",
  VACANCIES = "/vacancies",
  CREATE_RESUME = "/resume/create",
}

export default RoutePaths;
